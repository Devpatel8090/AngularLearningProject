import { ChangeValueDirective } from './change-value.directive';

describe('ChangeValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeValueDirective();
    expect(directive).toBeTruthy();
  });
});
